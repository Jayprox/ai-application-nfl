// Shared across every screen that groups/filters by position_group (Team
// detail's roster grouping, Player browse's filter dropdown) so the
// label text and display order stay identical everywhere.

export const POSITION_GROUP_ORDER = ['offense', 'defense', 'special_teams'];

export const POSITION_GROUP_LABEL = {
  offense: 'Offense',
  defense: 'Defense',
  special_teams: 'Special Teams',
};
